version: 2

submodules:
  include: all

sphinx:
    builder: html
    configuration: docs/conf.py

python:
   version: 3.7
   install:
      - requirements: docs/requirements.txt
      - method: pip
        path: .

